extends h5p-layout

block append scriptsBlock
    script!="window.H5PIntegration = parent.H5PIntegration || " + JSON.stringify(integration)

block content
    div(class="h5p-content", data-content-id=contentId)